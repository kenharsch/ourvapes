<h1 class = "center">Configuration</h1>
<br />
<div class = "center">
	<%= render "pair_compat_messages" %>
	<span id="parts-list">
		<table>
			<% @partslist.each do |part_type| %>
			<% present = !@conf_objects[part_type].nil? %>
			<tr>
				<td><%= part_type.capitalize %>:</td>
				<td><% if present %><%= @conf_objects[part_type].name %><% end %></td>
				<td>
					<% if present %>
					<% @add_or_change = "Change" %>
					<%= button_to "Remove", {action: "remove", controller: "configuration",
						remove: part_type, conf: @conf_objects}, class: "btn btn-primary"%>
					<% else %>
					<% @add_or_change = "Add" %>
					<% end %>
				</td>
				<td>
					<%= button_to @add_or_change, {action: "list", controller: "product_list",
						conf: @conf_objects}, class: "btn btn-primary"%>
				</td>
			</tr>
				<% if present %>
					<%= render "single_compat_messages", {part_type: part_type} %>
				<% end %>
			<% end %>
		</table>
	</span>
	<%= button_to "Clear", "configuration/clear", method: :get, class: "btn btn-primary",
		confirm: "Are you sure you want to clear all products?"%>
</div>