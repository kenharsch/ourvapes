<% if session[Constants::SESS_MY_CONFIG].part(prod.type).nil? %>
<% confirm_message = nil %>
<% else %>
<% confirm_message = "Do you want to overwrite your existing #{prod.type}?" %>
<% end %>
<%= button_to "Add to My Kit",
	{action: "add", controller: "configuration", add_id: prod.id},
	class: "btn btn-default btn-block", confirm: confirm_message %>
