<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
	<div class="container-fluid">
		<!-- Brand and toggle get grouped for better mobile display -->
		<div class="navbar-header">
			<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
				<span class="sr-only">Toggle navigation</span> <!-- TODO What is this for??? -->
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
				<span class="icon-bar"></span>
			</button>
			<a class="navbar-brand" href="/">VapeRater</a>
		</div>

		<!-- Collect the nav links, forms, and other content for toggling -->
		<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
			<ul class="nav navbar-nav">
				<li <%= "class = active" if @page == Constants::PAGE_PROD_LIST%>>
					<%= link_to "Products", {action: "list", controller: "/product_list"} %>
				</li>
			</ul>

			<%= form_tag "/product_list/list", method: "get", class: "navbar-form navbar-left" do %>
			<%= text_field_tag :query, @query, class: "form-control", placeholder: "Search" %>
			<%= submit_tag "GO", class: "btn btn-default", role: "button" %>
			<% end %>

			<!--
			<form class="navbar-form navbar-left" role="search" method="get" action="/product_list/list">
				<div class="form-group">
					<input type="text" class="form-control" placeholder="Search">
				</div>
				<button type="submit" class="btn btn-default">GO</button>
			</form>
		-->
		<ul class="nav navbar-nav navbar-right">
			<li <%= "class = active" if @page == Constants::PAGE_CONFIG%>>
				<%= link_to @my_config_label, {action: "index", controller: "/configuration"} %>
			</li>
			<% if user_signed_in? %>
				<li class="img-responsive">
					<%= image_tag gravatar_url(current_user, 48)%>
				</li>
			<% end %>
				<li><%= render 'shared/registration_items' %></li>
				<li><%= render 'shared/login_items' %></li>
		</ul>
	</div><!-- /.navbar-collapse -->
</div><!-- /.container-fluid -->
</nav>